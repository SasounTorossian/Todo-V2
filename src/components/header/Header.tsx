import { Box, Typography } from "@mui/material";

const Header = () => {
  return (
    <Box className="py-3">
      <Typography variant="h3">Todo Tracker V2</Typography>
    </Box>
  );
};

export default Header;
