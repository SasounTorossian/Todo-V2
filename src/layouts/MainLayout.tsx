import * as React from "react";
import TasksTable from "../components/TasksTable";

const MainLayout = () => {
  return (
    <React.Fragment>
      {/* TODO: Graph Area */}
      <TasksTable />
    </React.Fragment>
  );
};

export default MainLayout;
